 spring:
  application:
    name: subagent-personal-assistant-example
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen3.8-27b
          multi-model: true
 server:
   port: 8080
