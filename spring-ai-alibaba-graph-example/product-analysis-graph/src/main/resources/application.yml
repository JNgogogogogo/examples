spring:
  application:
    name: product-analysis-graph
  cloud:
    ai:
      dashscope:
        api-key: ${AI_DASHSCOPE_API_KEY}
        chat:
          options:
            model: qwen3.8-27b
            multi-model: true
server:
  port: 8080
