spring:
  application:
    name: product-analysis-graph
  cloud:
    ai:
      dashscope:
        api-key: ${AI_DASHSCOPE_API_KEY}
        chat:
          options:
            model: qwen3.8-27b
server:
  port: 8080
