spring:
  application:
    name: prompt-example

  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen3.8-27b
      # workspace-id: ${AI_DASHSCOPE_WORKSPACE_ID}
